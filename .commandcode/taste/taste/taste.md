# Taste
- Communicates in Indonesian (Bahasa Indonesia) and expects responses in the same language. Confidence: 0.9
- Prefers user-facing UI labels in navigation/sidebar to use full, descriptive names matching page titles rather than short/ambiguous abbreviations (e.g., renamed sidebar submenu "PR" to "Purchase Requisition" so the label matches the existing page header). Confidence: 0.3
- When presented with multiple-choice design options that include a recommended choice, tends to accept the recommended option (e.g., kanban with arrow buttons over adding a drag-and-drop library, automatic stock reservation on SO approval, full CRUD for activities, fixed-duration 8h session expiry over idle-only options, and building both Purchase Return UI entry points — "+ Tambah Return" form plus a "Buat Return" action in the Receiving detail modal — rather than a single entry point). Prefers solutions that avoid adding new third-party dependencies. Confidence: 0.85
- Works iteratively through a predefined sprint roadmap and wants the agent to continue to the next sprint (e.g., "lanjut sprint 5", "bisa lanjut?", "lanjut 7") once the current one is verified — expecting each sprint to be implemented end-to-end (backend + frontend + tests + docs) autonomously with minimal direction. Confidence: 0.9
- Values seeders being idempotent (rerunnable via `db:seed`) and test isolation (demo/seed data gated behind an env flag like `SEED_DEMO=false` in phpunit) so seed data never breaks the test suite. Confidence: 0.8
- Periodically asks for a status of remaining/pending work ("sisa pendingan apa saja?") and expects the answer to be grounded in project docs (e.g., `modul.md`) and the actual codebase — including gaps like backend features without UI — rather than a vague summary, so they can pick the next thing to work on. Confidence: 0.7
- Delegates code-implementable pending features (e.g., SO fulfillment, missing reservation UI, user/role management, quotation comparison) to the agent, while explicitly keeping operational tasks — UAT with end users, production deployment, and document approvals — out of the agent's scope for the user to handle themselves. Confidence: 0.6
- Expects completed work to be durably marked/tracked somewhere findable (asked "apakah kamu menandai pekerjaan yang sudah kamu lakukan? di mana kamu simpan?", then asked for a permanent module-status checklist doc) — not just ephemeral per-session todo lists; wants visibility into where progress is permanently recorded (e.g., README, docs/, git history). Confidence: 0.8
- Expects the session todo list to be kept in sync with actual completion state: after running verification (backend tests + frontend build), refresh the todo list so finished items are marked completed and only genuinely unfinished items remain pending ("jika sudah refresh todo nya ya"). Confidence: 0.6
- Wants project status captured in a durable checklist document (markdown in docs/, e.g. docs/07-Module-Checklist.md) that maps every module to a status (✅/⚠️/❌) with a detailed per-feature checklist including API endpoints, automated test results, and pending items ranked by priority — grounded in the actual codebase (e.g., verified via `php artisan route:list`) rather than memory. Confidence: 0.7
- Requests user-facing documentation as actual Word (.docx) files (e.g., "buatkan file docx"), not only markdown — expecting a polished, end-to-end usage guide that walks each business process from start to finish (master data setup → CRM/purchasing/production flows → reports → troubleshooting), with numbered steps, tables, and an example end-to-end scenario. Confidence: 0.6
- For list pages with filters, wants filters (e.g., product type: "bahan baku" / "produk jadi") implemented as select boxes placed directly beside the search input, alongside existing search + pagination. Also expects product-type filter selects to appear inside relevant modal forms (e.g., a "Tipe Produk" select next to the Gudang select that filters the items to count in the stock opname start modal), with selection state reset so filtered-out rows don't linger. Confidence: 0.7
- Cares about table compactness when vertical space is limited ("tampilkan se proporsional mungkin karena tempatnya terbatas"): wants related secondary info (e.g., supplier PIC phone under PIC name, supplier phone under email) composited into a single truncated column rather than separate wide columns, so the total column count stays small and the table stays proportional. When a column adds bulk, prefers folding the extra info as small secondary text under an existing action link (e.g., history count as "3× kontak" beneath the "Riwayat" link) rather than keeping a dedicated column. Also folds auxiliary figures (e.g., product HPP and per-unit margin shown as small text under the product select and price input) into existing empty space instead of adding new columns. Confidence: 0.8
- Wants modal form inputs laid out proportionally (fields sized/spanned to suit their content, e.g., two related fields side by side in a 2-column grid) with labels adjusted to fit actual needs, and wants form fields to include user guidance/hints (placeholder text explaining the expected input, e.g., "cth: SUP-001", and small hint text below each field) so users know how to fill each field correctly — the user repeatedly requests this for modal forms across pages (supplier form, lead create modal) and expects the shared form component to support proportional spans and hints so every page benefits. Also explicitly asks for a descriptive keterangan (explanation) on modal sections (e.g., a guide note under the Line Items title explaining that price auto-fills from product sale price and that internal HPP/margin is for reference only and not sent to the customer). Confidence: 0.9
- Wants business-critical fields in modal forms to be made mandatory (required labels with `*`, Save button disabled until they're filled) — e.g., when told to make category and unit required on the product form, those join SKU/name as gating fields. Confidence: 0.6
- Wants conditional form sections shown per selected type so users only see relevant fields: on the product add/edit modal, choosing "Produk Jadi" shows BOM + ingredients sections while "Bahan Baku" swaps them for a notes/description section — irrelevant fields are hidden entirely so the user isn't confused, and each section carries its own hint explaining what to fill. Confidence: 0.7
- Prefers simplifying workflow state machines by removing intermediate statuses when they don't add value (e.g., dropping the "contacted"/"Hubungi" step so new leads can go straight to "qualified"), relying on the backend already permitting the transition so the UI just exposes the direct action. Confidence: 0.5
- When describing a new feature conceptually (e.g., "bypass RFQ/Quotation with price & MOQ input, select via checklist, create PO"; "history contacted for leads — menunjukkan jumlahnya di tabel, beri tombol input, bagaimana menurutmu baiknya?"), invites the agent to propose the design and accepts the agent asking clarifying questions up front (PO grouping, menu name, relationship of history to lead status, outcome format) before building the full backend + frontend. Confidence: 0.6
- Prefers business-document actions to be genuinely functional rather than nominal: a button labeled to act (e.g., "Kirim") must actually do something — implement real sharing via a WhatsApp wa.me deep link with an auto-generated quote summary (also marking the record as sent), plus a browser-print (Cetak) option and a Detail-view modal for documents like sales quotations. Flags replacing actions that merely flip a DB status (draft→sent) without any real effect as a bad UX. Confidence: 0.6
- Wants line-item editors in add/edit modals to mirror the detail view: when the detail view shows a column field (e.g., discount_percent), the corresponding create-form's line items must expose the same field, and the grid needs visible column headers (e.g., Produk · Qty · Harga · Diskon % · Aksi) rather than an unlabeled row of inputs. Confidence: 0.5
- Prefers order-conversion/create flows (e.g., Quotation→Sales Order) to surface real stock availability before proceeding: after a warehouse is chosen, show a line-item table of required qty vs available qty per product (aggregating all batches), show a green "Cukup"/red "Kurang N" status per line, warn on shortages, and disable the create/convert action until stock is sufficient (or a different warehouse is selected) so users can't create orders that can't be fulfilled. Confidence: 0.6
- Keeps formal business-document templates/reference samples in the `docs/` folder and expects the agent to replicate that exact layout and wording verbatim when building print functionality (e.g., Cetak PDF untuk PO). Sources can be a reference PDF (e.g., "55. PURCHASE ORDER CV. RINDANG CEMARA SUKSES.pdf") or a plain-markdown wording file (e.g., "docs/wording surat po.md") storing the authoritative letter text (kop, clause wording like COA/Halal requirements, address/payment/signatory blocks). If a reference PDF can't be read (no pdftotext/Python), accepts a fallback: a standard Indonesian letter layout now, with an offer to write a small Node script to extract the sample's text for exact replication. Confidence: 0.55
- Prefers date displays formatted as Indonesian weekday + ISO date ("Senin, 2026-09-09") applied consistently to every date on a page (table columns, modal detail fields, WA messages, print documents). When formatting date-only strings, wants a helper that builds the weekday via Date.UTC with timeZone 'UTC' so the displayed day-of-week doesn't shift due to browser timezone. Also flags raw ISO datetime strings (e.g. "2026-09-03T00:00:00.000000Z") appearing in the UI as unfamiliar to non-technical users, so the date helper must tolerate full datetime values too (slice to just the yyyy-mm-dd part and show only the date with weekday) rather than leaking the raw timestamp. Confidence: 0.75
- Scopes product-picker dropdowns by business context: sales documents (Sales Quotation & Sales Order line items) should list only finished goods (`type=finished_good`), while purchasing documents (PR and PO product selects) should list only raw materials (`type=raw_material`), so users never pick an irrelevant product type in a form. Confidence: 0.6
- Wants any database-column field shown in a detail view to trace back to an actual data-entry point in the corresponding create/edit form — if a column exists in the data model (e.g., MFG/Expiry dates on a receiving line) but is never populated in the UI, it's seen as a gap ("tidak ada inputan nya saat receiving, dimana seharusnya kolom tersebut terisi?") and an input should be added so the column isn't left dead/always "—". When the agent proposes adding the missing input, the user actively requests it be built into the form ("tolong buatkan di form receiving ya"), confirming this as a real workflow preference. Confidence: 0.6
